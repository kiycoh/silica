"""Golden coherence harness — the curated vault is the golden label set.

Each probe masks one human-authored signal and asks the real pipeline function
to reproduce it. Run: ``uv run python -m evals.golden --vault <path>``.
"""
